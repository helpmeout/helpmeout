= helpmeout

To install add the gem to the test environment, change spec/spec.opts to contain --format Helpmeout::Formatter and create a file helpmeout.yaml in the projects root cotaining:
  exclude_prefixes:
    - path prefixes that should be replaced in the stacktrace
  project_root: the root directory of your project
